# Jenny - data generator
Generate sample data, based on fields and field types

- input: json with the structure: field name, field type, limits(later)
- data output: json, jsonl, csv, etc
- sink output: stdout, fs, kafka, etc